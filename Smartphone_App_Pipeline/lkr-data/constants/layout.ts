/** iPhone 13 logical size (points). Used to size and center content on wider screens. */
export const IPHONE_13_LAYOUT_WIDTH = 390;
export const IPHONE_13_LAYOUT_HEIGHT = 844;

/** Horizontal inset from the edges of the 390pt column (matches common iPhone margins). */
export const IPHONE_13_CONTENT_HORIZONTAL_INSET = 20;

/** Additional top padding below the safe area (tab screens, modal). */
export const CONTENT_TOP_EXTRA = 20;

/** Top padding below the parallax header before the first content block (Explore). */
export const PARALLAX_CONTENT_TOP_PADDING = 24 + CONTENT_TOP_EXTRA;

/** Default iOS tab bar height (points), for bottom padding under scroll content. */
export const TAB_BAR_HEIGHT = 49;
